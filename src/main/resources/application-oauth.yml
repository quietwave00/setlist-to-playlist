spotify:
  client:
    id: ${SPOTIFY_CLIENT_ID}
    secret: ${SPOTIFY_CLIENT_SECRET}
  redirect:
    uri: ${SPOTIFY_REDIRECT_URI}

youtube:
  client:
    id: ${YOUTUBE_CLIENT_ID}
    secret: ${YOUTUBE_CLIENT_SECRET}
  redirect:
    uri: ${YOUTUBE_REDIRECT_URI}
